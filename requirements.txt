# Root requirements: installs the shared core library + application dependencies
# Railway's NIXPACKS builder auto-detects this file and runs pip install
-e ./packages/core
-r ./packages/lookalike-prospecting/requirements.txt
