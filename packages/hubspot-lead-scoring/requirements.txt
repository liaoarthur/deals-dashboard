# HubSpot Lead Scoring — dependencies
# Note: sqlite3 is included in Python's standard library, no extra package needed.
flask>=3.0.0
flask-cors>=4.0.0
requests>=2.31.0
python-dotenv==1.0.1
anthropic>=0.40.0
pyyaml>=6.0
